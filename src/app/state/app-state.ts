import { GlobalState } from './global/global.state';
import { AuthState } from './auth/auth.state';
import { StagesState } from './Stages/stages.state';

export const AppState = [GlobalState, AuthState, StagesState];
